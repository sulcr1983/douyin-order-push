# 订单同步系统使用说明

## 系统功能

- 自动读取CSV订单文件
- 清洗和处理订单数据
- 同步数据到企业微信
- 自动记录和更新订单状态

## 快速开始

1、需要在企业微信-智能表格/飞书/钉钉自己先创建智能/多维表格，然后通过右上角三横-接收外部数据，把webhook地址和示例数据都提交给python，让python匹配清洗你的原来订单和需要同步到订单即可。\
2、在项目文件夹下建一个csv文件夹，把订单导出的表放进去csv文件。

### 方法一：双击运行（推荐）

1. 找到并双击 `运行订单同步.bat` 文件
2. 系统会自动执行同步操作
3. 操作完成后会显示结果，按任意键关闭窗口

### 方法二：手动运行

1. 打开命令提示符（CMD）
2. 切换到脚本所在目录
3. 执行命令：`mail.py`

## 配置说明

### 1. 配置文件（.env）

用记事本打开 `.env` 文件，修改以下配置：

```
# 企业微信Webhook URL
WECOM_WEBHOOK_URL=https://qyapi.weixin.qq.com/cgi-bin/wedoc/smartsheet/webhook?key=你的Webhook密钥

# 订单存放文件夹
CSV_FOLDER_PATH=./csv
```

### 2. 订单文件

- 将订单CSV文件放入 `csv` 文件夹中
- 系统会自动识别最新的CSV文件
- 支持的编码格式：utf-8-sig、gbk

## 常见问题

### 问题1：双击批处理文件没有反应

- 检查是否已安装Python
- 检查Python是否添加到系统环境变量
- 检查 `.env` 文件配置是否正确

### 问题2：运行时出现错误

- 查看命令窗口中的错误信息
- 检查CSV文件格式是否正确
- 检查网络连接是否正常

### 问题3：企业微信没有收到数据

- 检查Webhook URL是否正确
- 检查网络连接是否正常
- 查看命令窗口中的错误信息

## 技术支持

如遇问题，请联系系统管理员sulcr\@qq.com。
